import { ClickBoomDirective } from './click-boom.directive';

describe('ClickBoomDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickBoomDirective();
    expect(directive).toBeTruthy();
  });
});
